[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "apex-sam"
version = "0.1.0"
description = "Public implementation of APEX-SAM inference and evaluation"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "torch",
  "torchvision",
  "segment-anything",
  "opencv-python",
  "scikit-image",
  "scipy",
  "SimpleITK",
  "matplotlib",
  "scikit-learn",
]

[project.scripts]
apex-sam-preprocess = "apex_sam.cli.preprocess_dataset:main"
apex-sam-infer = "apex_sam.cli.inference:main"
apex-sam-eval = "apex_sam.cli.eval:main"

[tool.setuptools.packages.find]
include = ["apex_sam*"]
